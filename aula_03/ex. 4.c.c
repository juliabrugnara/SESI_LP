#include <stdio.h>

int main() {
    // PQ: Começamos em 10, a condicao e ser maior que 1, e usamos 'i--' (subtrair).
    for(int i = 10; i >= 1; i--) {
        printf("%d ", i);
    }
    return 0;
}